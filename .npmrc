# Mitigate supply-chain attacks (e.g. compromised package versions published
# and yanked within hours): refuse to resolve any registry version younger
# than 7 days. Requires npm >= 11.10.0, see engines.npm in package.json.
min-release-age=7
engine-strict=true
